{
  "name": "pv4-timing",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "node --import tsx --test test/*.test.ts",
    "cdk": "cdk",
    "harness": "node --import tsx test/harness.ts",
    "snapshot": "node --import tsx scripts/record-snapshot.ts"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.163",
    "@types/node": "^24.10.1",
    "aws-cdk": "2.1142.0",
    "esbuild": "^0.28.2",
    "tsx": "^4.23.0",
    "typescript": "~7.0.2"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.35.0",
    "@aws-lambda-powertools/metrics": "^2.35.0",
    "@aws-sdk/client-dynamodb": "^3.1136.0",
    "@aws-sdk/lib-dynamodb": "^3.1136.0",
    "aws-cdk-lib": "^2.269.0",
    "constructs": "^10.5.0"
  }
}
